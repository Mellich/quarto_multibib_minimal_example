# Minimal Example Qarto multibib

To reproduce:

- Quarto version: 1.3.450
- Add multibib extension: `quarto add mellich/pandoc-multibib`
- Build project: `quarto render`

In `html` output, references are not visible.
